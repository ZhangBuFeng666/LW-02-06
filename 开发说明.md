# 接力开发说明

## 启动方式

先启动 Mock 后端：

```bash
npm run mock
```

再启动前端：

```bash
npm run dev
```

访问地址：

- 前台：http://127.0.0.1:5173/
- 后台：http://127.0.0.1:5173/admin/login
- Mock API：http://127.0.0.1:5174/api/health

## 测试账号

- 前台用户：user / 123456
- 后台管理员：admin / 123456
- 后台运营：operator / 123456

## 核心数据结构

商品字段：id、name、price、categoryId、stock、status、img、desc。

订单状态：unpaid、paid、shipped、received。

后台角色：admin 可以管理商品，operator 只能查看。

## 接口约定

- GET /api/goods：商品列表
- GET /api/goods/:id：商品详情
- POST /api/login：用户登录
- POST /api/register：用户注册
- POST /api/admin/login：后台登录
- POST /api/admin/goods：新增商品
- PUT /api/admin/goods/:id：编辑商品
- DELETE /api/admin/goods/:id：删除商品
- PATCH /api/admin/goods/:id/status：上架/下架
- GET /api/cart?userId=1：购物车
- POST /api/cart：加入购物车
- PUT /api/cart/:id：修改购物车
- DELETE /api/cart/:id：删除购物车
- POST /api/orders：创建订单
- GET /api/orders?userId=1：用户订单
- GET /api/orders/:id：订单详情
- PATCH /api/orders/:id/pay：支付
- PATCH /api/orders/:id/ship：发货
- PATCH /api/orders/:id/receive：确认收货

- GET /api/addresses?userId=1：地址列表
- POST /api/addresses：新增地址
- PUT /api/addresses/:id：编辑地址
- DELETE /api/addresses/:id：删除地址
- PATCH /api/addresses/:id/default：设置默认地址
- GET /api/favorites?userId=1：收藏列表
- POST /api/favorites：收藏商品
- DELETE /api/favorites/:id：取消收藏
- GET /api/reviews?goodId=1：商品评价
- POST /api/reviews：发布评价
- GET /api/orders/:id/logistics：物流轨迹

## 接力分工建议

组长已完成第一版：Mock 后端、service 层、路由和主链路。

组员 A 接首页、分类页、商品详情页、搜索、轮播、热门商品。
需要对不同大小屏幕进行适配。需要补充商品类别，商品个数，商品图片（注意图片大小，项目打包后小于8M），
优化商品展示和页面样式。可以参考京东，淘宝
| 作为顾客，我希望…           | 功能                  |
| ------------------- | ------------------- |
| 进入商城首页时能被主视觉和商品宣传吸引 | 首页首屏、搜索框、本周推荐、热门商品  |
| 能按分类快速浏览不同类型商品      | 分类浏览、商品筛选、商品卡片展示    |
| 商品详情页能清楚展示商品信息      | 图片、价格、库存、描述、评价、购买按钮 |
| 在手机上也能正常浏览和操作商城     | 响应式布局、移动端适配         |

首页搜索框可搜索。
热门商品卡片优化。可看清图片，价格，按钮。
分类页布局优化。
商品详情页图片、价格、库存、按钮优化。
移动端适配 

组员 B 接注册，登录、购物车、创建订单、支付、订单列表、订单详情和我的页面。参考老师的作业要求与京东淘宝等应用，比如说我的页面，补充地址，历史订单，待付款，待收货，退换等。
| 作为顾客，我希望…                | 功能          |
| ------------------------ | ----------- |
| 能注册账号并登录商城               | 用户注册、登录     |
| 能把喜欢的商品加入购物车，并修改数量、删除或结算 | 购物车管理       |
| 创建订单时能选择收货地址             | 地址管理、默认地址选择 |
| 提交订单后能进入支付页并看到订单金额       | 订单创建、支付     |
| 在订单详情中查看商品、地址、物流状态和物流轨迹  | 订单查询、物流跟踪   |
| 能收藏商品并在收藏夹中查看            | 商品收藏管理      |

订单创建。创建订单时可新建或选择地址。
支付成功后返回订单列表并刷新，订单倒计时
订单列表和详情优化。

组员 C 接后台登录、权限提示、商品管理表单、上下架和可选订单发货。参考https://github.com/macrozheng/mall 商品/分类/订单/ 三选一即可
| 作为管理员和，我希望…          | 功能               |
| ------------------ | ---------------- |
| 后台管理商品后，前台商品展示实时更新 | 商品新增、编辑、删除、上下架联动 |
| 后台发货后，订单详情能显示物流信息  | 发货管理、物流同步展示      |

后台登录样式。
admin/operator 权限显示。


组员 D 接测试用例、Bug 记录、Report、PPT 和答辩演示脚本。

## 不建议随意改动

- API 路径
- service 函数名
- 商品字段名
- 订单状态值
- 后台角色值

这些内容会影响多个模块，确实要改时由组长统一调整。

